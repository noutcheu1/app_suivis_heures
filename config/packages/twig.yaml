twig:
    file_name_pattern: '*.twig'
    globals:
        # Service d'auth accessible dans tous les templates (ex. nom affiché dans le menu).
        auth_service: '@App\Service\AuthService'

when@test:
    twig:
        strict_variables: true
