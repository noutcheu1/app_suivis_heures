-- ============================================================
-- 02_horaire.sql — (désactivé)
-- ============================================================
-- La base bdchaudoudoux_horaire est désormais construite à 100 %
-- par les migrations Doctrine (doctrine:migrations:migrate).
--
--   - users_suivi  → créée par la migration initiale Doctrine
--   - compte admin → inséré par docker/entrypoint.sh APRÈS migrate
--
-- Ce fichier est laissé vide pour ne pas entrer en conflit avec
-- les migrations.
-- ============================================================

SELECT 'bdchaudoudoux_horaire : schéma géré par les migrations Doctrine' AS message;
